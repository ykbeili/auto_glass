<!DOCTYPE html>
<html>
  <head>
    <title>AutoGlassWebsite</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <nav class="container">
      <div class="row">
        <% if customer_signed_in? %>
          <div class="col-3 d-flex justify-content-center">
            <%= link_to "Profile", edit_customer_registration_path, class:'mr-5' %>
            <%= button_to "Log out", destroy_customer_session_path, method: :delete %>
          </div>
        <% else %>
          <div class="col-3 d-flex justify-content-center">
            <%= link_to "Login", new_customer_session_path, class:'mr-5' %>
            <%= button_to "Sign Up", new_customer_registration_path %>
          </div>
        <% end %>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>

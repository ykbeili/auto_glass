<!DOCTYPE html>
<html>
  <head>
    <title>AutoGlassWebsite</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <% if customer_signed_in? %>
      <%= link_to "Profile", edit_customer_registration_path %>
      <%= button_to "Log out", destroy_customer_session_path, method: :delete %>
    <% else %>
      <%= link_to "Login", new_customer_session_path %>
      <%= button_to "Sign Up", new_customer_registration_path %>
    <% end %>
    <%= yield %>
  </body>
</html>
